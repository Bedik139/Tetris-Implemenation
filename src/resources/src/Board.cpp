#include <iostream>
#include <string>
#include <vector>
#include <cstdlib>
#include <ctime>
/**
 * Generates a tree of block.
 */
class Board {
    public:
        // Constructor
        block();

        void generate(){

        };

    private:
        std::vector<std::vector<int>> const BOARD;
};